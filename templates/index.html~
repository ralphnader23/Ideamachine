<!-- mess around starting point --!>

<!-- can we make a mako template do something? --!>


<div class="wrapper">
    % for row in rows:
    <div class="hello">Hello ${row}</div>!
    % endfor
</div>

