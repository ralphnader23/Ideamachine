<!-- mess around starting point --!>

<!-- can we make a mako template do something? --!>


<div class="wrapper">
    % for row in rows:
    <div class="name">Name: ${row['name']}!</div>
    <div class="age">Age: ${['age']}<br><br></div>
    % endfor
</div>

